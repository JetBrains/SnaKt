# Testing swarm agent instructions

Execute issue #337 as an independent SnaKt testing agent. Probe reads and
writes of mutable fields through shared and unique references across calls and
branches, using minimal two-field classes and positive plus negative or
near-boundary controls. Follow the golden-test protocol, investigate and
minimize suspected defects, report only previously unreported confirmed bugs,
and deliver the tests in a PR targeting `implementing-air-automations`.

