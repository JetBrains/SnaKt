# Conflict solving instruction

Monitor automation-authored pull requests and resolve merge conflicts when they appear. For pull request #421, compare `test/swarm-339-inheritance-dispatch` with its `implementing-air-automations` base, preserve both branches' intended changes, verify the resolution, and leave signed commits on the source branch for the platform to push.
