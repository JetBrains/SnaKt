# Conflict solving

Monitor automation-authored pull requests and resolve merge conflicts when they appear. For this run, inspect PR #396 (`test/issue-337-mutable-field-permissions` into `implementing-air-automations`), resolve any conflicts on the source branch, verify the result, and leave signed commits for platform delivery.

