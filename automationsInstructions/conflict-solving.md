# Conflict solving

Monitor automation pull requests and resolve merge conflicts when they appear. For this run, inspect PR #499 (`test/issue-468-failure-propagation` into `implementing-air-automations`), resolve any conflicts on its source branch, verify the result, and leave signed commits for platform delivery.
