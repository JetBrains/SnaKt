# Conflict solving

Monitor automation pull requests and resolve merge conflicts when they appear. For this run, inspect PR #402 (`test/issue-330-string-operation-models` into `implementing-air-automations`), resolve any conflicts on the source branch, verify the result, and leave committed changes for the platform to push.
