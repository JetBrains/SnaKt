# Conflict solving

Monitor automation pull requests and resolve merge conflicts when they appear. For this run, inspect PR #502 (`test/issue-452-collection-rewrites` into `implementing-air-automations`) and resolve any conflicts on its source branch.
