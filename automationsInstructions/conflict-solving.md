# Conflict solving

Monitor automation-authored pull requests and resolve merge conflicts when they appear. For this run, inspect PR #532 (`test/issue-445-heap-aliasing`) against its base branch, `implementing-air-automations`, and commit a conflict resolution on the source branch only if one is required.

