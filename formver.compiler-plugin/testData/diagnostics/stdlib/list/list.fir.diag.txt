/list.kt:(77,88): info: Generated Viper text for declaration:
field size: Ref

method declaration() returns (ret0: Ref)
  ensures isSubtype(typeOf(ret0), unitType())
{
  var l1: Ref
  var l2: Ref
  var l3: Ref
  label lbl_ret
  inhale isSubtype(typeOf(ret0), unitType())
}

/list.kt:(187,201): info: Generated Viper text for initialization:
field size: Ref

method emptyList() returns (ret: Ref)
  ensures isSubtype(typeOf(ret), List())
  ensures acc(ret.size, write)
  ensures intFromRef(ret.size) >= 0
  ensures acc(List_pred_shared(ret), wildcard)
  ensures intFromRef(ret.size) == 0


method initialization(l: Ref) returns (ret0: Ref)
  requires acc(l.size, write)
  requires intFromRef(l.size) >= 0
  requires isSubtype(typeOf(l), List())
  ensures acc(l.size, write)
  ensures intFromRef(l.size) >= 0
  ensures isSubtype(typeOf(ret0), unitType())
{
  var myList: Ref
  var myEmptyList: Ref
  inhale acc(List_pred_shared(l), wildcard)
  myList := l
  myEmptyList := emptyList()
  label lbl_ret
  inhale isSubtype(typeOf(ret0), unitType())
}

/list.kt:(297,304): info: Generated Viper text for add_get:
field size: Ref

method add(this: Ref, element: Ref) returns (ret: Ref)
  requires acc(this.size, write)
  requires intFromRef(this.size) >= 0
  requires isSubtype(typeOf(this), MutableList())
  requires isSubtype(typeOf(element), intType())
  ensures acc(this.size, write)
  ensures intFromRef(this.size) >= 0
  ensures isSubtype(typeOf(ret), boolType())
  ensures intFromRef(this.size) == old(intFromRef(this.size)) + 1


method add_get(l: Ref) returns (ret0: Ref)
  requires acc(l.size, write)
  requires intFromRef(l.size) >= 0
  requires isSubtype(typeOf(l), MutableList())
  ensures acc(l.size, write)
  ensures intFromRef(l.size) >= 0
  ensures isSubtype(typeOf(ret0), unitType())
{
  var anon: Ref
  var n: Ref
  inhale acc(MutableList_pred_shared(l), wildcard)
  anon := add(l, intToRef(1))
  n := get(l, intToRef(0))
  label lbl_ret
  inhale isSubtype(typeOf(ret0), unitType())
}

method get(this: Ref, index: Ref) returns (ret: Ref)
  requires acc(this.size, write)
  requires intFromRef(this.size) >= 0
  requires isSubtype(typeOf(this), MutableList())
  requires isSubtype(typeOf(index), intType())
  requires intFromRef(index) >= 0
  requires intFromRef(this.size) > intFromRef(index)
  ensures acc(this.size, write)
  ensures intFromRef(this.size) >= 0
  ensures isSubtype(typeOf(ret), intType())
  ensures intFromRef(this.size) == old(intFromRef(this.size))


/list.kt:(379,391): info: Generated Viper text for last_or_null:
field prop_size: Ref

method get(this: Ref, index: Ref) returns (ret: Ref)
  requires acc(this.prop_size, write)
  requires intFromRef(this.prop_size) >= 0
  requires isSubtype(typeOf(this), List())
  requires isSubtype(typeOf(index), intType())
  requires intFromRef(index) >= 0
  requires intFromRef(this.prop_size) > intFromRef(index)
  ensures acc(this.prop_size, write)
  ensures intFromRef(this.prop_size) >= 0
  ensures isSubtype(typeOf(ret), intType())
  ensures intFromRef(this.prop_size) == old(intFromRef(this.prop_size))


method last_or_null(l: Ref) returns (ret0: Ref)
  requires acc(l.prop_size, write)
  requires intFromRef(l.prop_size) >= 0
  requires isSubtype(typeOf(l), List())
  ensures acc(l.prop_size, write)
  ensures intFromRef(l.prop_size) >= 0
  ensures isSubtype(typeOf(ret0), nullable(intType()))
{
  var size: Ref
  inhale acc(List_pred_shared(l), wildcard)
  size := l.prop_size
  inhale isSubtype(typeOf(size), intType())
  if (!(intFromRef(size) == 0)) {
    var anon: Ref
    anon := get(l, minusInts(size, intToRef(1)))
    ret0 := anon
    goto lbl_ret
  } else {
    ret0 := nullValue()
    goto lbl_ret
  }
  label lbl_ret
}

/list.kt:(545,553): info: Generated Viper text for is_empty:
field size: Ref

method get(this: Ref, index: Ref) returns (ret: Ref)
  requires acc(this.size, write)
  requires intFromRef(this.size) >= 0
  requires isSubtype(typeOf(this), List())
  requires isSubtype(typeOf(index), intType())
  requires intFromRef(index) >= 0
  requires intFromRef(this.size) > intFromRef(index)
  ensures acc(this.size, write)
  ensures intFromRef(this.size) >= 0
  ensures isSubtype(typeOf(ret), intType())
  ensures intFromRef(this.size) == old(intFromRef(this.size))


method isEmpty(this: Ref) returns (ret: Ref)
  requires acc(this.size, write)
  requires intFromRef(this.size) >= 0
  requires isSubtype(typeOf(this), List())
  ensures acc(this.size, write)
  ensures intFromRef(this.size) >= 0
  ensures isSubtype(typeOf(ret), boolType())
  ensures intFromRef(this.size) == old(intFromRef(this.size))
  ensures boolFromRef(ret) ==> intFromRef(this.size) == 0
  ensures !boolFromRef(ret) ==> intFromRef(this.size) > 0


method is_empty(l: Ref) returns (ret0: Ref)
  requires acc(l.size, write)
  requires intFromRef(l.size) >= 0
  requires isSubtype(typeOf(l), List())
  ensures acc(l.size, write)
  ensures intFromRef(l.size) >= 0
  ensures isSubtype(typeOf(ret0), intType())
{
  var anon: Ref
  inhale acc(List_pred_shared(l), wildcard)
  anon := isEmpty(l)
  if (!boolFromRef(anon)) {
    ret0 := get(l, intToRef(0))
  } else {
    ret0 := intToRef(1)}
  goto lbl_ret
  label lbl_ret
}

/list.kt:(670,683): info: Generated Viper text for nullable_list:
field size: Ref

method get(this: Ref, index: Ref) returns (ret: Ref)
  requires acc(this.size, write)
  requires intFromRef(this.size) >= 0
  requires isSubtype(typeOf(this), List())
  requires isSubtype(typeOf(index), intType())
  requires intFromRef(index) >= 0
  requires intFromRef(this.size) > intFromRef(index)
  ensures acc(this.size, write)
  ensures intFromRef(this.size) >= 0
  ensures isSubtype(typeOf(ret), intType())
  ensures intFromRef(this.size) == old(intFromRef(this.size))


method isEmpty(this: Ref) returns (ret: Ref)
  requires acc(this.size, write)
  requires intFromRef(this.size) >= 0
  requires isSubtype(typeOf(this), List())
  ensures acc(this.size, write)
  ensures intFromRef(this.size) >= 0
  ensures isSubtype(typeOf(ret), boolType())
  ensures intFromRef(this.size) == old(intFromRef(this.size))
  ensures boolFromRef(ret) ==> intFromRef(this.size) == 0
  ensures !boolFromRef(ret) ==> intFromRef(this.size) > 0


method nullable_list(l: Ref) returns (ret0: Ref)
  requires l != nullValue() ==> acc(l.size, write)
  requires l != nullValue() ==> intFromRef(l.size) >= 0
  requires isSubtype(typeOf(l), nullable(List()))
  ensures l != nullValue() ==> acc(l.size, write)
  ensures l != nullValue() ==> intFromRef(l.size) >= 0
  ensures isSubtype(typeOf(ret0), unitType())
{
  var anon0: Ref
  inhale l != nullValue() ==> acc(List_pred_shared(l), wildcard)
  if (!(l == nullValue())) {
    var anon1: Ref
    anon1 := isEmpty(l)
    anon0 := notBool(anon1)
  } else {
    anon0 := boolToRef(false)}
  if (boolFromRef(anon0)) {
    var x: Ref
    var anon: Ref
    anon := l.size
    inhale isSubtype(typeOf(anon), intType())
    x := get(l, minusInts(anon, intToRef(1)))
  }
  label lbl_ret
  inhale isSubtype(typeOf(ret0), unitType())
}
