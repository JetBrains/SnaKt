/binary_search.kt:(90,108): info: Generated Viper text for safe_binary_search:
field prop_size: Ref

method get(this: Ref, index: Ref) returns (ret: Ref)
  requires acc(this.prop_size, write)
  requires intFromRef(this.prop_size) >= 0
  requires isSubtype(typeOf(this), List())
  requires isSubtype(typeOf(index), intType())
  requires intFromRef(index) >= 0
  requires intFromRef(this.prop_size) > intFromRef(index)
  ensures acc(this.prop_size, write)
  ensures intFromRef(this.prop_size) >= 0
  ensures isSubtype(typeOf(ret), intType())
  ensures intFromRef(this.prop_size) == old(intFromRef(this.prop_size))


method isEmpty(this: Ref) returns (ret: Ref)
  requires acc(this.prop_size, write)
  requires intFromRef(this.prop_size) >= 0
  requires isSubtype(typeOf(this), List())
  ensures acc(this.prop_size, write)
  ensures intFromRef(this.prop_size) >= 0
  ensures isSubtype(typeOf(ret), boolType())
  ensures intFromRef(this.prop_size) == old(intFromRef(this.prop_size))
  ensures boolFromRef(ret) ==> intFromRef(this.prop_size) == 0
  ensures !boolFromRef(ret) ==> intFromRef(this.prop_size) > 0


method safe_binary_search(arr: Ref, target: Ref) returns (ret0: Ref)
  requires acc(arr.prop_size, write)
  requires intFromRef(arr.prop_size) >= 0
  requires isSubtype(typeOf(arr), List())
  requires isSubtype(typeOf(target), intType())
  ensures acc(arr.prop_size, write)
  ensures intFromRef(arr.prop_size) >= 0
  ensures isSubtype(typeOf(ret0), boolType())
{
  var size: Ref
  var mid: Ref
  var anon0: Ref
  var anon1: Ref
  inhale acc(List_pred_shared(arr), wildcard)
  size := arr.prop_size
  inhale isSubtype(typeOf(size), intType())
  anon0 := arr.prop_size
  inhale isSubtype(typeOf(anon0), intType())
  mid := divInts(anon0, intToRef(2))
  anon1 := isEmpty(arr)
  if (boolFromRef(anon1)) {
    ret0 := boolToRef(false)
  } else {
    var anon2: Ref
    anon2 := get(arr, mid)
    if (intFromRef(anon2) == intFromRef(target)) {
      ret0 := boolToRef(true)
    } else {
      var anon3: Ref
      anon3 := get(arr, mid)
      if (intFromRef(anon3) < intFromRef(target)) {
        var anon4: Ref
        anon4 := subList(arr, plusInts(mid, intToRef(1)), size)
        ret0 := safe_binary_search(anon4, target)
      } else {
        var anon: Ref
        anon := subList(arr, intToRef(0), mid)
        ret0 := safe_binary_search(anon, target)
      }
    }
  }
  goto lbl_ret
  label lbl_ret
}

method subList(this: Ref, fromIndex: Ref, toIndex: Ref) returns (ret: Ref)
  requires acc(this.prop_size, write)
  requires intFromRef(this.prop_size) >= 0
  requires isSubtype(typeOf(this), List())
  requires isSubtype(typeOf(fromIndex), intType())
  requires isSubtype(typeOf(toIndex), intType())
  requires intFromRef(fromIndex) <= intFromRef(toIndex)
  requires intFromRef(fromIndex) >= 0
  requires intFromRef(toIndex) <= intFromRef(this.prop_size)
  ensures acc(this.prop_size, write)
  ensures intFromRef(this.prop_size) >= 0
  ensures isSubtype(typeOf(ret), List())
  ensures acc(ret.prop_size, write)
  ensures intFromRef(ret.prop_size) >= 0
  ensures acc(List_pred_shared(ret), wildcard)
  ensures intFromRef(this.prop_size) == old(intFromRef(this.prop_size))
  ensures intFromRef(ret.prop_size) ==
    intFromRef(toIndex) - intFromRef(fromIndex)


/binary_search.kt:(537,563): info: Generated Viper text for unsafe_binary_search_fixed:
field size: Ref

method get(this: Ref, index: Ref) returns (ret: Ref)
  requires acc(this.size, write)
  requires intFromRef(this.size) >= 0
  requires isSubtype(typeOf(this), List())
  requires isSubtype(typeOf(index), intType())
  requires intFromRef(index) >= 0
  requires intFromRef(this.size) > intFromRef(index)
  ensures acc(this.size, write)
  ensures intFromRef(this.size) >= 0
  ensures isSubtype(typeOf(ret), intType())
  ensures intFromRef(this.size) == old(intFromRef(this.size))


method unsafe_binary_search_fixed(arr: Ref, target: Ref, left: Ref, right: Ref)
  returns (ret0: Ref)
  requires acc(arr.size, write)
  requires intFromRef(arr.size) >= 0
  requires isSubtype(typeOf(arr), List())
  requires isSubtype(typeOf(target), intType())
  requires isSubtype(typeOf(left), intType())
  requires isSubtype(typeOf(right), intType())
  ensures acc(arr.size, write)
  ensures intFromRef(arr.size) >= 0
  ensures isSubtype(typeOf(ret0), boolType())
{
  var anon0: Ref
  var anon1: Ref
  var mid: Ref
  var anon3: Ref
  inhale acc(List_pred_shared(arr), wildcard)
  if (intFromRef(left) > intFromRef(right)) {
    anon1 := boolToRef(true)
  } else {
    anon1 := ltInts(left, intToRef(0))}
  if (boolFromRef(anon1)) {
    anon0 := boolToRef(true)
  } else {
    var anon2: Ref
    anon2 := arr.size
    inhale isSubtype(typeOf(anon2), intType())
    anon0 := geInts(right, anon2)
  }
  if (boolFromRef(anon0)) {
    ret0 := boolToRef(false)
    goto lbl_ret
  }
  mid := plusInts(left, divInts(minusInts(right, left), intToRef(2)))
  anon3 := get(arr, mid)
  if (intFromRef(anon3) == intFromRef(target)) {
    ret0 := boolToRef(true)
  } else {
    var anon: Ref
    anon := get(arr, mid)
    if (intFromRef(anon) < intFromRef(target)) {
      ret0 := unsafe_binary_search_fixed(arr, target, plusInts(mid, intToRef(1)),
        right)
    } else {
      ret0 := unsafe_binary_search_fixed(arr, target, left, minusInts(mid, intToRef(1)))}
  }
  goto lbl_ret
  label lbl_ret
}
