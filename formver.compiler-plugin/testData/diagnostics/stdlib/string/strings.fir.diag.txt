/strings.kt:(237,245): info: Generated Viper text for testType:
field bf_str: Ref

field size: Ref

predicate BooleanArray_pred_shared(this: Ref) {
  acc(this.size, wildcard) && isSubtype(typeOf(this.size), intType()) &&
  acc(Cloneable_pred_shared(this), wildcard) &&
  acc(Serializable_pred_shared(this), wildcard)
}

predicate BooleanArray_pred_unique(this: Ref) {
  acc(this.size, wildcard) && isSubtype(typeOf(this.size), intType()) &&
  acc(Cloneable_pred_unique(this), write) &&
  acc(Serializable_pred_unique(this), write)
}

predicate Cloneable_pred_shared(this: Ref) {
  true
}

predicate Cloneable_pred_unique(this: Ref) {
  true
}

predicate Serializable_pred_shared(this: Ref) {
  true
}

predicate Serializable_pred_unique(this: Ref) {
  true
}

predicate StringBox_pred_shared(this: Ref) {
  acc(this.bf_str, wildcard) &&
  isSubtype(typeOf(this.bf_str), stringType())
}

predicate StringBox_pred_unique(this: Ref) {
  acc(this.bf_str, wildcard) &&
  isSubtype(typeOf(this.bf_str), stringType())
}

method con(str: Ref) returns (ret: Ref)
  requires isSubtype(typeOf(str), stringType())
  ensures isSubtype(typeOf(ret), StringBox())
  ensures acc(StringBox_pred_shared(ret), wildcard)
  ensures acc(StringBox_pred_unique(ret), write)
  ensures stringFromRef((unfolding acc(StringBox_pred_shared(ret), wildcard) in
      ret.bf_str)) ==
    stringFromRef(str)


method testType(s: Ref) returns (ret0: Ref)
  requires isSubtype(typeOf(s), stringType())
  ensures isSubtype(typeOf(ret0), unitType())
{
  var cond1: Ref
  var anon0: Ref
  var anon1: Ref
  var cond2: Ref
  var anon2: Ref
  var anon: Ref
  anon1 := con(s)
  unfold acc(StringBox_pred_shared(anon1), wildcard)
  anon0 := anon1.bf_str
  cond1 := boolToRef(stringFromRef(anon0) == stringFromRef(s))
  anon := con(stringToRef(Seq(115, 116, 114)))
  unfold acc(StringBox_pred_shared(anon), wildcard)
  anon2 := anon.bf_str
  cond2 := boolToRef(stringFromRef(anon2) == Seq(115, 116, 114))
  assert boolFromRef(cond1)
  assert boolFromRef(cond2)
  label lbl_ret
  inhale isSubtype(typeOf(ret0), unitType())
}

/strings.kt:(412,427): info: Generated Viper text for testLengthField:
field bf_str: Ref

field size: Ref

predicate BooleanArray_pred_shared(this: Ref) {
  acc(this.size, wildcard) && isSubtype(typeOf(this.size), intType()) &&
  acc(Cloneable_pred_shared(this), wildcard) &&
  acc(Serializable_pred_shared(this), wildcard)
}

predicate BooleanArray_pred_unique(this: Ref) {
  acc(this.size, wildcard) && isSubtype(typeOf(this.size), intType()) &&
  acc(Cloneable_pred_unique(this), write) &&
  acc(Serializable_pred_unique(this), write)
}

predicate Cloneable_pred_shared(this: Ref) {
  true
}

predicate Cloneable_pred_unique(this: Ref) {
  true
}

predicate Serializable_pred_shared(this: Ref) {
  true
}

predicate Serializable_pred_unique(this: Ref) {
  true
}

predicate StringBox_pred_shared(this: Ref) {
  acc(this.bf_str, wildcard) &&
  isSubtype(typeOf(this.bf_str), stringType())
}

predicate StringBox_pred_unique(this: Ref) {
  acc(this.bf_str, wildcard) &&
  isSubtype(typeOf(this.bf_str), stringType())
}

method con(str: Ref) returns (ret: Ref)
  requires isSubtype(typeOf(str), stringType())
  ensures isSubtype(typeOf(ret), StringBox())
  ensures acc(StringBox_pred_shared(ret), wildcard)
  ensures acc(StringBox_pred_unique(ret), write)
  ensures stringFromRef((unfolding acc(StringBox_pred_shared(ret), wildcard) in
      ret.bf_str)) ==
    stringFromRef(str)


method testLengthField(s: Ref) returns (ret0: Ref)
  requires isSubtype(typeOf(s), stringType())
  ensures isSubtype(typeOf(ret0), unitType())
{
  var len: Ref
  var cond1: Ref
  var anon0: Ref
  var anon: Ref
  len := stringLength(s)
  anon := con(stringToRef(Seq(115, 116, 114)))
  unfold acc(StringBox_pred_shared(anon), wildcard)
  anon0 := anon.bf_str
  cond1 := boolToRef(|stringFromRef(anon0)| == 3)
  assert boolFromRef(cond1)
  label lbl_ret
  inhale isSubtype(typeOf(ret0), unitType())
}

/strings.kt:(552,559): info: Generated Viper text for testOps:
field size: Ref

predicate BooleanArray_pred_shared(this: Ref) {
  acc(this.size, wildcard) && isSubtype(typeOf(this.size), intType()) &&
  acc(Cloneable_pred_shared(this), wildcard) &&
  acc(Serializable_pred_shared(this), wildcard)
}

predicate BooleanArray_pred_unique(this: Ref) {
  acc(this.size, wildcard) && isSubtype(typeOf(this.size), intType()) &&
  acc(Cloneable_pred_unique(this), write) &&
  acc(Serializable_pred_unique(this), write)
}

predicate Cloneable_pred_shared(this: Ref) {
  true
}

predicate Cloneable_pred_unique(this: Ref) {
  true
}

predicate Serializable_pred_shared(this: Ref) {
  true
}

predicate Serializable_pred_unique(this: Ref) {
  true
}

method plus(this: Ref, other: Ref) returns (ret: Ref)
  requires isSubtype(typeOf(this), stringType())
  requires isSubtype(typeOf(other), nullable(anyType()))
  ensures isSubtype(typeOf(ret), stringType())


method testOps(s: Ref) returns (ret0: Ref)
  requires isSubtype(typeOf(s), stringType())
  ensures isSubtype(typeOf(ret0), unitType())
{
  var c: Ref
  var anon: Ref
  var str: Ref
  var helloWorld: Ref
  var stringPlusInteger: Ref
  if (|stringFromRef(s)| > 0) {
    c := stringGet(s, intToRef(0))
  } else {
    c := charToRef(97)}
  if (charFromRef(c) == 97) {
    anon := boolToRef(true)
  } else {
    anon := gtInts(stringLength(s), intToRef(0))}
  assert boolFromRef(anon)
  str := stringToRef(Seq(97, 98, 97))
  assert stringFromRef(str)[0] == stringFromRef(str)[2]
  assert !(stringFromRef(str)[1] == stringFromRef(str)[0])
  assert stringFromRef(str)[1] == 98
  assert Seq(75, 111, 116, 108, 105, 110, 46, 83, 116, 114, 105, 110, 103) ==
    Seq(75, 111, 116, 108, 105, 110, 46, 83, 116, 114, 105, 110, 103)
  helloWorld := addStringChar(stringToRef(Seq(72, 101, 108, 108, 111, 32, 87,
    111, 114, 108, 100)), charToRef(33))
  assert stringFromRef(helloWorld) ==
    Seq(72, 101, 108, 108, 111, 32, 87, 111, 114, 108, 100, 33)
  stringPlusInteger := plus(stringToRef(Seq(52, 50)), intToRef(42))
  label lbl_ret
  inhale isSubtype(typeOf(ret0), unitType())
}
