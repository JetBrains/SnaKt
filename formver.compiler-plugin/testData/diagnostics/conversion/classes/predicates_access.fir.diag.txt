/predicates_access.kt:(249,272): info: Generated Viper text for accessSuperTypeProperty:
field a: Ref

field b: Ref

field x: Ref

field y: Ref

predicate A_pred_shared(this: Ref) {
  acc(this.a, wildcard) && isSubtype(typeOf(this.a), intType())
}

predicate A_pred_unique(this: Ref) {
  acc(this.a, wildcard) && isSubtype(typeOf(this.a), intType())
}

predicate B_pred_shared(this: Ref) {
  acc(this.b, wildcard) && isSubtype(typeOf(this.b), intType()) &&
  acc(A_pred_shared(this), wildcard)
}

predicate B_pred_unique(this: Ref) {
  acc(this.b, wildcard) && isSubtype(typeOf(this.b), intType()) &&
  acc(A_pred_unique(this), write)
}

predicate C_pred_shared(this: Ref) {
  acc(this.x, wildcard) && acc(A_pred_shared(this.x), wildcard) &&
  isSubtype(typeOf(this.x), A()) &&
  acc(D_pred_shared(this), wildcard) &&
  acc(B_pred_shared(this), wildcard)
}

predicate C_pred_unique(this: Ref) {
  acc(this.x, wildcard) && acc(A_pred_shared(this.x), wildcard) &&
  isSubtype(typeOf(this.x), A()) &&
  acc(this.y, write) &&
  acc(A_pred_shared(this.y), wildcard) &&
  isSubtype(typeOf(this.y), A()) &&
  acc(D_pred_unique(this), write) &&
  acc(B_pred_unique(this), write)
}

predicate D_pred_shared(this: Ref) {
  true
}

predicate D_pred_unique(this: Ref) {
  true
}

method accessSuperTypeProperty(c: Ref) returns (ret0: Ref)
  requires isSubtype(typeOf(c), C())
  ensures isSubtype(typeOf(ret0), unitType())
{
  var temp: Ref
  inhale acc(C_pred_shared(c), wildcard)
  unfold acc(C_pred_shared(c), wildcard)
  unfold acc(B_pred_shared(c), wildcard)
  unfold acc(A_pred_shared(c), wildcard)
  temp := c.a
  label lbl_ret
  inhale isSubtype(typeOf(ret0), unitType())
}

method d(this: Ref) returns (ret: Ref)


/predicates_access.kt:(306,318): info: Generated Viper text for accessNested:
field a: Ref

field b: Ref

field x: Ref

field y: Ref

predicate A_pred_shared(this: Ref) {
  acc(this.a, wildcard) && isSubtype(typeOf(this.a), intType())
}

predicate A_pred_unique(this: Ref) {
  acc(this.a, wildcard) && isSubtype(typeOf(this.a), intType())
}

predicate B_pred_shared(this: Ref) {
  acc(this.b, wildcard) && isSubtype(typeOf(this.b), intType()) &&
  acc(A_pred_shared(this), wildcard)
}

predicate B_pred_unique(this: Ref) {
  acc(this.b, wildcard) && isSubtype(typeOf(this.b), intType()) &&
  acc(A_pred_unique(this), write)
}

predicate C_pred_shared(this: Ref) {
  acc(this.x, wildcard) && acc(A_pred_shared(this.x), wildcard) &&
  isSubtype(typeOf(this.x), A()) &&
  acc(D_pred_shared(this), wildcard) &&
  acc(B_pred_shared(this), wildcard)
}

predicate C_pred_unique(this: Ref) {
  acc(this.x, wildcard) && acc(A_pred_shared(this.x), wildcard) &&
  isSubtype(typeOf(this.x), A()) &&
  acc(this.y, write) &&
  acc(A_pred_shared(this.y), wildcard) &&
  isSubtype(typeOf(this.y), A()) &&
  acc(D_pred_unique(this), write) &&
  acc(B_pred_unique(this), write)
}

predicate D_pred_shared(this: Ref) {
  true
}

predicate D_pred_unique(this: Ref) {
  true
}

method accessNested(c: Ref) returns (ret0: Ref)
  requires isSubtype(typeOf(c), C())
  ensures isSubtype(typeOf(ret0), unitType())
{
  var temp: Ref
  var anon: Ref
  inhale acc(C_pred_shared(c), wildcard)
  unfold acc(C_pred_shared(c), wildcard)
  anon := c.x
  unfold acc(A_pred_shared(anon), wildcard)
  temp := anon.a
  label lbl_ret
  inhale isSubtype(typeOf(ret0), unitType())
}

method d(this: Ref) returns (ret: Ref)


/predicates_access.kt:(354,368): info: Generated Viper text for accessNullable:
field a: Ref

predicate A_pred_shared(this: Ref) {
  acc(this.a, wildcard) && isSubtype(typeOf(this.a), intType())
}

predicate A_pred_unique(this: Ref) {
  acc(this.a, wildcard) && isSubtype(typeOf(this.a), intType())
}

method accessNullable(x: Ref) returns (ret0: Ref)
  requires isSubtype(typeOf(x), nullable(A()))
  ensures isSubtype(typeOf(ret0), unitType())
{
  var n: Ref
  inhale x != nullValue() ==> acc(A_pred_shared(x), wildcard)
  if (!(x == nullValue())) {
    unfold acc(A_pred_shared(x), wildcard)
    n := x.a
  }
  label lbl_ret
  inhale isSubtype(typeOf(ret0), unitType())
}

/predicates_access.kt:(442,452): info: Generated Viper text for accessCast:
field a: Ref

field b: Ref

predicate A_pred_shared(this: Ref) {
  acc(this.a, wildcard) && isSubtype(typeOf(this.a), intType())
}

predicate A_pred_unique(this: Ref) {
  acc(this.a, wildcard) && isSubtype(typeOf(this.a), intType())
}

predicate B_pred_shared(this: Ref) {
  acc(this.b, wildcard) && isSubtype(typeOf(this.b), intType()) &&
  acc(A_pred_shared(this), wildcard)
}

predicate B_pred_unique(this: Ref) {
  acc(this.b, wildcard) && isSubtype(typeOf(this.b), intType()) &&
  acc(A_pred_unique(this), write)
}

method accessCast(x: Ref) returns (ret0: Ref)
  requires isSubtype(typeOf(x), A())
  ensures isSubtype(typeOf(ret0), unitType())
{
  var n: Ref
  inhale acc(A_pred_shared(x), wildcard)
  inhale isSubtype(typeOf(x), B())
  inhale acc(B_pred_shared(x), wildcard)
  unfold acc(B_pred_shared(x), wildcard)
  n := x.b
  label lbl_ret
  inhale isSubtype(typeOf(ret0), unitType())
}

/predicates_access.kt:(501,515): info: Generated Viper text for accessSafeCast:
field a: Ref

field b: Ref

predicate A_pred_shared(this: Ref) {
  acc(this.a, wildcard) && isSubtype(typeOf(this.a), intType())
}

predicate A_pred_unique(this: Ref) {
  acc(this.a, wildcard) && isSubtype(typeOf(this.a), intType())
}

predicate B_pred_shared(this: Ref) {
  acc(this.b, wildcard) && isSubtype(typeOf(this.b), intType()) &&
  acc(A_pred_shared(this), wildcard)
}

predicate B_pred_unique(this: Ref) {
  acc(this.b, wildcard) && isSubtype(typeOf(this.b), intType()) &&
  acc(A_pred_unique(this), write)
}

method accessSafeCast(x: Ref) returns (ret0: Ref)
  requires isSubtype(typeOf(x), A())
  ensures isSubtype(typeOf(ret0), unitType())
{
  var n: Ref
  var y: Ref
  inhale acc(A_pred_shared(x), wildcard)
  n := intToRef(0)
  if (isSubtype(typeOf(x), B())) {
    y := x
  } else {
    y := nullValue()}
  inhale isSubtype(typeOf(y), nullable(B()))
  inhale y != nullValue() ==> acc(B_pred_shared(y), wildcard)
  if (!(y == nullValue())) {
    unfold acc(B_pred_shared(y), wildcard)
    n := y.b
  }
  label lbl_ret
  inhale isSubtype(typeOf(ret0), unitType())
}

/predicates_access.kt:(612,627): info: Generated Viper text for accessSmartCast:
field a: Ref

field b: Ref

predicate A_pred_shared(this: Ref) {
  acc(this.a, wildcard) && isSubtype(typeOf(this.a), intType())
}

predicate A_pred_unique(this: Ref) {
  acc(this.a, wildcard) && isSubtype(typeOf(this.a), intType())
}

predicate B_pred_shared(this: Ref) {
  acc(this.b, wildcard) && isSubtype(typeOf(this.b), intType()) &&
  acc(A_pred_shared(this), wildcard)
}

predicate B_pred_unique(this: Ref) {
  acc(this.b, wildcard) && isSubtype(typeOf(this.b), intType()) &&
  acc(A_pred_unique(this), write)
}

method accessSmartCast(x: Ref) returns (ret0: Ref)
  requires isSubtype(typeOf(x), A())
  ensures isSubtype(typeOf(ret0), unitType())
{
  var n: Ref
  inhale acc(A_pred_shared(x), wildcard)
  n := intToRef(0)
  if (isSubtype(typeOf(x), B())) {
    inhale acc(B_pred_shared(x), wildcard)
    unfold acc(B_pred_shared(x), wildcard)
    n := x.b
  }
  label lbl_ret
  inhale isSubtype(typeOf(ret0), unitType())
}
