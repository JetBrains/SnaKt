/unique_predicates.kt:(269,283): info: Generated Viper text for unique_foo_arg:
field w: Ref

field x: Ref

field y: Ref

field z: Ref

predicate Foo_pred_shared(this: Ref) {
  acc(this.w, wildcard) && isSubtype(typeOf(this.w), intType()) &&
  acc(this.y, wildcard) &&
  acc(T_pred_shared(this.y), wildcard) &&
  isSubtype(typeOf(this.y), T()) &&
  acc(S_pred_shared(this), wildcard)
}

predicate Foo_pred_unique(this: Ref) {
  acc(this.w, wildcard) && isSubtype(typeOf(this.w), intType()) &&
  acc(this.x, write) &&
  isSubtype(typeOf(this.x), intType()) &&
  acc(this.y, wildcard) &&
  acc(T_pred_shared(this.y), wildcard) &&
  acc(T_pred_unique(this.y), write) &&
  isSubtype(typeOf(this.y), T()) &&
  acc(this.z, write) &&
  acc(T_pred_shared(this.z), wildcard) &&
  acc(T_pred_unique(this.z), write) &&
  isSubtype(typeOf(this.z), T()) &&
  acc(S_pred_unique(this), write)
}

predicate S_pred_shared(this: Ref) {
  true
}

predicate S_pred_unique(this: Ref) {
  true
}

predicate T_pred_shared(this: Ref) {
  true
}

predicate T_pred_unique(this: Ref) {
  true
}

method unique_foo_arg(foo: Ref) returns (ret0: Ref)
  requires isSubtype(typeOf(foo), Foo())
  requires acc(Foo_pred_unique(foo), write)
  ensures isSubtype(typeOf(ret0), unitType())
{
  inhale acc(Foo_pred_shared(foo), wildcard)
  label lbl_ret
  inhale isSubtype(typeOf(ret0), unitType())
}

/unique_predicates.kt:(310,329): info: Generated Viper text for nullable_unique_arg:
predicate T_pred_shared(this: Ref) {
  true
}

predicate T_pred_unique(this: Ref) {
  true
}

method nullable_unique_arg(p_t: Ref) returns (ret0: Ref)
  requires isSubtype(typeOf(p_t), nullable(T()))
  requires p_t != nullValue() ==> acc(T_pred_unique(p_t), write)
  ensures isSubtype(typeOf(ret0), unitType())
{
  inhale p_t != nullValue() ==> acc(T_pred_shared(p_t), wildcard)
  label ret
  inhale isSubtype(typeOf(ret0), unitType())
}

/unique_predicates.kt:(353,372): info: Generated Viper text for borrowed_unique_arg:
predicate T_pred_shared(this: Ref) {
  true
}

predicate T_pred_unique(this: Ref) {
  true
}

method borrowed_unique_arg(p_t: Ref) returns (ret0: Ref)
  requires isSubtype(typeOf(p_t), T())
  requires acc(T_pred_unique(p_t), write)
  ensures acc(T_pred_unique(p_t), write)
  ensures isSubtype(typeOf(ret0), unitType())
{
  inhale acc(T_pred_shared(p_t), wildcard)
  label ret
  inhale isSubtype(typeOf(ret0), unitType())
}

/unique_predicates.kt:(424,439): info: Generated Viper text for unique_receiver:
predicate T_pred_shared(this_dispatch: Ref) {
  true
}

predicate T_pred_unique(this_dispatch: Ref) {
  true
}

method unique_receiver(this: Ref) returns (ret0: Ref)
  requires isSubtype(typeOf(this), T())
  requires acc(T_pred_unique(this), write)
  ensures isSubtype(typeOf(ret0), unitType())
{
  inhale acc(T_pred_shared(this), wildcard)
  label ret
  inhale isSubtype(typeOf(ret0), unitType())
}

/unique_predicates.kt:(488,512): info: Generated Viper text for borrowed_unique_receiver:
predicate T_pred_shared(this_dispatch: Ref) {
  true
}

predicate T_pred_unique(this_dispatch: Ref) {
  true
}

method borrowed_unique_receiver(this: Ref) returns (ret0: Ref)
  requires isSubtype(typeOf(this), T())
  requires acc(T_pred_unique(this), write)
  ensures acc(T_pred_unique(this), write)
  ensures isSubtype(typeOf(ret0), unitType())
{
  inhale acc(T_pred_shared(this), wildcard)
  label ret
  inhale isSubtype(typeOf(ret0), unitType())
}

/unique_predicates.kt:(531,544): info: Generated Viper text for unique_result:
predicate T_pred_shared(this: Ref) {
  true
}

predicate T_pred_unique(this: Ref) {
  true
}

method con() returns (ret: Ref)
  ensures isSubtype(typeOf(ret), T())
  ensures acc(T_pred_shared(ret), wildcard)
  ensures acc(T_pred_unique(ret), write)


method unique_result() returns (ret0: Ref)
  ensures isSubtype(typeOf(ret0), T())
  ensures acc(T_pred_shared(ret0), wildcard)
  ensures acc(T_pred_unique(ret0), write)
{
  ret0 := con()
  goto lbl_ret
  label lbl_ret
}

/unique_predicates.kt:(579,601): info: Generated Viper text for unique_nullable_result:
predicate T_pred_shared(this: Ref) {
  true
}

predicate T_pred_unique(this: Ref) {
  true
}

method unique_nullable_result() returns (ret0: Ref)
  ensures isSubtype(typeOf(ret0), nullable(T()))
  ensures ret0 != nullValue() ==> acc(T_pred_shared(ret0), wildcard)
  ensures ret0 != nullValue() ==> acc(T_pred_unique(ret0), write)
{
  ret0 := nullValue()
  goto ret
  label ret
}
