/predicate_collection_base_collision.kt:(114,124): info: Generated Viper text for p$shared:
function p$shared(x: Ref): Ref
  requires isSubtype(typeOf(x), intType())
  ensures isSubtype(typeOf(result), intType())
{
  x
}

/predicate_collection_base_collision.kt:(147,179): info: Generated Viper text for predicateCollectionBaseCollision:
field size: Ref

function f$p$shared(x: Ref): Ref
  requires isSubtype(typeOf(x), intType())
  ensures isSubtype(typeOf(result), intType())


predicate Collection$p$shared(this: Ref) {
  acc(p$shared(this), wildcard)
}

predicate Collection$p$unique(this: Ref) {
  acc(p$unique(this), write)
}

predicate List$p$shared(this: Ref) {
  acc(Collection$p$shared(this), wildcard)
}

predicate List$p$unique(this: Ref) {
  acc(Collection$p$unique(this), write)
}

predicate p$shared(this: Ref) {
  true
}

predicate p$unique(this: Ref) {
  true
}

method isEmpty(this: Ref) returns (ret: Ref)
  requires acc(this.size, write)
  requires intFromRef(this.size) >= 0
  requires isSubtype(typeOf(this), List())
  ensures acc(this.size, write)
  ensures intFromRef(this.size) >= 0
  ensures isSubtype(typeOf(ret), boolType())
  ensures intFromRef(this.size) == old(intFromRef(this.size))
  ensures boolFromRef(ret) ==> intFromRef(this.size) == 0
  ensures !boolFromRef(ret) ==> intFromRef(this.size) > 0


method predicateCollectionBaseCollision(xs: Ref) returns (ret$0: Ref)
  requires acc(xs.size, write)
  requires intFromRef(xs.size) >= 0
  requires isSubtype(typeOf(xs), List())
  ensures acc(xs.size, write)
  ensures intFromRef(xs.size) >= 0
  ensures isSubtype(typeOf(ret$0), unitType())
{
  var anon: Ref
  inhale acc(List$p$shared(xs), wildcard)
  anon := isEmpty(xs)
  label lbl$ret$0
  inhale isSubtype(typeOf(ret$0), unitType())
}
