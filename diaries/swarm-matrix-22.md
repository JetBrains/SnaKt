# Swarm matrix 22 diary

## Run e50f9b43-708b-4dff-a20c-795cb5e8337f

- Read `AUTOMATIONS.md` before taking repository actions.
- Recorded the issue #452 assignment in `automationsInstructions/swarm-matrix-22.md`.
- Confirmed the checkout began on `implementing-air-automations` at `9bac7b3` with only this run's new instruction file untracked.
- Inspected recent pull requests targeting `implementing-air-automations`; their branch convention includes `test/issue-<number>-<topic>`. Created `test/issue-452-collection-rewrites` from the source branch.
- Confirmed GitHub CLI authentication is available as `jetbrains-air[bot]`.
- Used semantic code search for existing list, array, indexing, size, and bounds coverage. The closest tests are `stdlib/list/list.kt` and `verification/contracts/negative/list.kt`; they establish positive non-empty guards, mutable-list add/get behavior, and negative empty/out-of-bounds behavior. A direct testData scan found no Kotlin object-array or primitive-array coverage in verification diagnostics.
- Read `docs/agents-dev.md`; development will use conversion-only focused runs first, followed by focused verification, with every golden update reviewed as an observation.
- Inspected issue #328 and PR #395 before choosing array probes. That prior work already classifies missing native-array bounds as the specification gap in closed issue #253 and operator-form indexed writes as the conversion failure in closed issue #252.
- Added `collection_metamorphic_rewrites.kt` with paired rewrites covering `List` guarded reads (`!isEmpty()`/`isEmpty() == false`, operator/call syntax), inline/named empty-list reads, literal/named mutable-list add then read, equivalent guarded explicit `MutableList.set` calls, an unchecked set boundary control, and operator/call reads on object and primitive arrays.
- The initial focused run under the host JDK 25.0.2 failed during Gradle configuration. Downloaded the `eclipse-temurin:21-jdk` container image, extracted its JDK under `/tmp`, and reran with Java 21.0.12.
- A deliberately tried operator-form `list[0] = value` produced the known internal conversion error from issue #252. Its detail contains a nondeterministic FIR object identity hash, so removed that probe from the committed golden and retained the stable equivalent explicit-call probes. This outcome is classified as a known internal error, not a new defect.
- Ran the conversion-only focused test. After the normal first-run creation of the FIR golden, `./agent-scripts/test.sh collection_metamorphic_rewrites` passed.
- The first verification attempt was a backend/harness setup failure because Z3 was absent. Downloaded the repository-documented Z3 4.8.7 release under `/tmp`, set `Z3_EXE`, restarted Gradle, and reran.
- Verification classified both guarded-list rewrites, both mutable add/read rewrites, both guarded explicit-set rewrites, and both object/primitive array operator/call rewrites as supported and verified. The paired forms retain equivalent relevant specifications and outcomes despite local/binder and surface-syntax differences.
- Verification classified both empty-list rewrites as the same expected proof failure (`POSSIBLE_INDEX_OUT_OF_BOUND`), with matching upper-bound diagnostics. These are the negative controls.
- The unchecked explicit `MutableList.set(0, value)` boundary control verifies because its generic `set` signature has no index bounds or element-state model. Operator assignment instead fails conversion. Searched open and closed issues using `MutableList set index bounds`, `list set out of bounds`, and `set indexing verification`; closed issue #252 already documents that indexed writes lack a `set` precondition/postcondition and element model for `MutableList` and arrays, so no duplicate bug was filed.
- Object-array and `IntArray` reads likewise use generic signatures without bounds, making the parameter probes (which admit empty arrays) verify. This matches prior PR #395 and closed issue #253; no duplicate bug was filed.
- Ran `./agent-scripts/test.sh --update-goldens collection_metamorphic_rewrites` with Java 21 and Z3 4.8.7. It rewrote no goldens, reported exactly the two intended empty-list warnings, regenerated the test registration, and passed `check-testdata.sh`. Read the complete Kotlin source, FIR golden, and verification golden and confirmed those observations are intentional.
- Ran `./agent-scripts/check-all.sh` with Java 21 and Z3 4.8.7. Gradle `check` and `check-testdata.sh` passed; the wrapper exited 2 solely because `pre-commit` was unavailable. A user-level install was blocked by the externally managed Python environment, and an isolated virtual-environment install was blocked by the proxy denying PyPI downloads. Read `.pre-commit-config.yaml`, then ran its local hooks (`agent-scripts/check-testdata.sh` and `agent-scripts/tests/run.sh`) directly plus `git diff --check`; all passed. The remaining upstream hook is the end-of-file fixer, so checked the changed text files for final newlines before delivery.
- Committed the focused probes, goldens, generated registration, instruction record, and diary as `8f9e867`, pushed `test/issue-452-collection-rewrites`, and opened pull request #502 against `implementing-air-automations`.
- Applied the requested `swarmTestingDone` label to triggering issue #452 after delivery completed.

Produced by Air Automations. Name: Testing swarm agent / Run: https://air.jetbrains.cloud/org/05cf1a7f-6ab5-713b-abd3-29d0c8a05e2d/automations/34ccbbdd-3fd2-474e-8f0a-10b4b2d5bda4?run=e50f9b43-708b-4dff-a20c-795cb5e8337f
