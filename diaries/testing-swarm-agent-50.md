# Testing swarm agent 50 diary

- Read `AUTOMATIONS.md` before taking any other repository action.
- Recorded the issue #367 assignment in `automationsInstructions/testing-swarm-agent-50.md` as required.
- Inspected repository status and confirmed the checkout started on `implementing-air-automations` at `9bac7b3` with only this run's new instruction and diary files untracked.
- Used semantic code search to locate annotation selection logic and related golden tests. The declaration checker gives `NeverConvert` precedence over `AlwaysVerify` for conversion, and gives `NeverConvert`, then `NeverVerify`, precedence over `AlwaysVerify` for verification.
- Reviewed annotation targets, test-fixture selection configuration, existing `NeverVerify`, `NeverConvert`, `AlwaysVerify`, and `Pure` cases, and recent PR branch names.
- Created branch `test/issue-367-annotation-selection` from `implementing-air-automations`, following the repository's existing `test/issue-...` convention.
- Added an initial `annotation_selection.kt` probe covering conflicting annotations on one declaration, purity obligations under `NeverVerify` and `NeverConvert`, and opposite selection annotations on nested local functions.
- The first fast-loop attempt could not start under the environment's Java 25 runtime. Confirmed project CI uses Java 21, downloaded Temurin 21 through the authenticated GitHub API, and reran with that runtime.
- Read the complete initial conversion and golden-update reports. Observed that `AlwaysVerify` plus `NeverVerify` still converts but skips verification; `AlwaysVerify` plus `NeverConvert` does not convert; an impure `Pure` function under `NeverVerify` still reports its purity violation; the same function under `NeverConvert` is excluded; and a local `AlwaysVerify` function nested in a `NeverConvert` function is independently converted.
- Found and minimized a conversion defect: a selected outer function that references a local `NeverConvert` function fails with an internal error identifying the excluded local FIR declaration. Searched open and closed GitHub issues for `NeverConvert` local-function and annotation internal-error variants and found no duplicate.
- Removed that unstable internal-error case from the annotation-selection golden (the error text contains a nondeterministic object identity) and retained it as the minimal reproducer for a separate bug report. A second probe showed that merely declaring any local function inside a converted outer function reaches the same unsupported conversion path, independent of annotation or invocation. Moved the nested `NeverVerify` boundary control into the excluded outer function, where local declarations are independently selected without converting the enclosing body.
- The stable nested test passed both conversion-only and full verification even though the nested `AlwaysVerify` function's generated Viper contains `assert false`. Traced this to the test `VerificationFacade`: its FIR visitor adds a `FirSimpleFunction` for verification but does not visit that function's children, so only outermost functions reach the verifier. Production verification occurs directly in the declaration checker, making this specifically a false-positive gap in the full test pipeline.
- Searched open and closed issues for `AlwaysVerify` local functions, nested functions not verified, `VerificationFacade` local handling, and local-declaration verifier tests; found no duplicate. Added a top-level `AlwaysVerify` function with the same failing assertion as a positive control, so the golden demonstrates that the verifier reports the top-level obligation while missing the nested one.
- Installed the repository-pinned Z3 4.8.7 in a temporary tool directory after the first real verifier invocation identified it as missing.
- Read the complete final golden-update report. It records one verification warning for the top-level `AlwaysVerify` positive control, conversion output for both nested functions, no verification warning for the nested `AlwaysVerify` function, the purity violation under `NeverVerify`, and total exclusion under `NeverConvert`. These observations match the intended coverage and expose the nested-verification gap rather than treating regeneration as proof of correctness.
- Opened GitHub issue #386, “Full test pipeline skips verification of selected local functions,” with the minimal reproducer, expected and observed behavior, root-cause location, revision/runtime details, the `swarmTestingBug` label, and this run's signature.
- Reran `./agent-scripts/test.sh annotation_selection` and `./agent-scripts/test.sh --verify annotation_selection` with Java 21 and Z3 4.8.7; both ran one test and passed.
- Ran `./agent-scripts/check-all.sh`. Gradle checks and test-data checks passed, but the first run correctly returned exit 2 because `pre-commit` was absent. The system Python refused a user install and the environment proxy blocked PyPI, so downloaded pre-commit's official standalone release from GitHub. Its hook environment also needed an unavailable PyPI dependency that the configured end-of-file hook does not use; used the standalone bundle's local setuptools wheel and omitted that unused dependency in the temporary hook cache. Ran all configured hooks successfully, then reran `check-all.sh` to a clean exit 0: Gradle check, test-data checks, end-of-file fixer, test-data hook, and script tests all passed.

Produced by Air Automations. Name: Testing swarm agent / Run: https://air.jetbrains.cloud/org/05cf1a7f-6ab5-713b-abd3-29d0c8a05e2d/automations/34ccbbdd-3fd2-474e-8f0a-10b4b2d5bda4?run=26a4321f-5388-4783-8a12-f83a588dea97
