# Testing swarm agent 459 diary

## Assignment

Test collections, arrays, and indexing for determinism and repeatability under issue #459. Use focused golden-file probes and controls, inspect all golden updates, verify outcomes, search existing issues before any bug report, and deliver through a pull request to `implementing-air-automations`.

## Actions and evidence

- Read `AUTOMATIONS.md` before taking repository actions.
- Recorded the assignment in `automationsInstructions/testing-swarm-agent-459.md` and started this diary as required.
- Confirmed the checkout was at revision `9bac7b3` on `implementing-air-automations`, inspected recent pull requests, and created `test/issue-459-collections-determinism` from that source branch.
- Inspected `StdLibConverter`, special-property handling, and nearby list tests. The modeled collection operations are `List.get`, `List.subList`, `Collection.isEmpty`, `emptyList`, and `MutableList.add`; array reads and size use type-only generic call contracts, while indexed writes and `arrayOf` conversion are unsupported.
- Added `collections_determinism.kt` with exact-repeat and harmlessly perturbed positive controls for guarded first-element reads and `MutableList.add` followed by index zero, plus repeated direct/indirect reads from an empty list as boundary controls.
- Added `arrays_determinism.kt` with repeated and perturbed generic-array reads, sizes, and writes; primitive-array reads and size; an empty-array boundary; and a singleton `arrayOf` control.
- The initial test attempt was a harness/environment failure: the provided JDK 25.0.2 is incompatible with this Gradle build. Downloaded Temurin JDK 21.0.12.1 to `/tmp`, matching CI's Java major version, and reran. Full verification initially reported a backend setup failure because Z3 was absent; downloaded the repository's required Z3 4.8.7 to `/tmp` and reran with `Z3_EXE` set.
- Developed with `./agent-scripts/test.sh <pattern>` and regenerated with `--update-goldens`. Read the complete source markers and both generated Viper goldens. List `get` carries `0 <= index < size`, size preservation, and nonnegative size facts. Array/primitive-array `get` and `size` carry only receiver/result type facts. Array writes consistently report `INTERNAL_ERROR` at the assignment; `arrayOf(29)` consistently reports the existing unsupported-vararg internal error at `29`.
- Full verification classified five list controls as supported and verified. Both empty-list accesses are expected proof failures, emitted in source order with `POSSIBLE_INDEX_OUT_OF_BOUND` at the indexed expression. Array reads, primitive-array reads, size functions, and `emptyArray<Int>()[0]` all report successful verification; the empty read is unsound because no bounds obligation exists.
- Searched open and closed GitHub issues for array bounds, `Array.get`, `IntArray`, indexed writes, and array size. The unsound array-read result is already covered by open issues #482 and #490. Closed issue #252 covers indexed-write internal errors, and closed issue #253 covers missing array-size nonnegativity. No duplicate bug issue was filed.
- Repeated the complete verification sequence twice for each targeted test (`arrays_determinism`, `collections_determinism`, then both again). All four isolated invocations exited 0 and SHA-256 manifests before and after were identical.
- Ran `./agent-scripts/check-all.sh`. Its full-suite ordering exposed nondeterminism hidden by isolated repetition: `arrays_determinism.fir.diag.txt` expected `FirUnitExpression@3fecb076` / `@ea0c99c`, but the unchanged conversion emitted `@30094d11` / `@4c9c0251`. `./agent-scripts/test.sh` without a pattern reproduced the failure and recovered that exact normalized diff. This is an unstable `INTERNAL_ERROR` diagnostic caused by JVM identity hashes in FIR node rendering.
- Searched again for the diagnostic-instability root cause. Open issue #374 already reports nondeterministic FIR identity hashes in `INTERNAL_ERROR` text, and #494 reports the same behavior for another unsupported FIR node; combined with #252's indexed-write report, this result is already covered. No duplicate issue was filed.
- Removed the array-write and `arrayOf` cases from the committed golden suite because their known identity-hash diagnostics cannot be stable in a full run. Their probes, outcomes, and evidence remain recorded above. Retained stable generic/primitive reads and size probes, the statically empty array boundary, and all list controls.
- Regenerated the reduced array golden, read the complete reported change, and ran the unfiltered fast loop: all 173 conversion tests passed. Reran `./agent-scripts/check-all.sh`: Gradle `check` passed (including the full verifier suite) and `check-testdata.sh` passed. The script exited 2 only because `pre-commit` was unavailable. Attempted the required installation in an isolated virtual environment, but the package proxy rejected `files.pythonhosted.org` with HTTP 403. Ran the repository's local pre-commit hooks directly instead: `agent-scripts/check-testdata.sh` and `agent-scripts/tests/run.sh` both passed. All changed text files end with a newline and `git diff --check` passed.
- Committed the probes and records as `9f1996a`, pushed `test/issue-459-collections-determinism`, and opened pull request #539 against `implementing-air-automations`. Added and verified the required `swarmTestingDone` label on triggering issue #459.

Produced by Air Automations. Name: Testing swarm agent / Run: https://air.jetbrains.cloud/org/05cf1a7f-6ab5-713b-abd3-29d0c8a05e2d/automations/34ccbbdd-3fd2-474e-8f0a-10b4b2d5bda4?run=97078c97-b259-479e-9d81-6cbfc4081c5e
